project.ext.versionName="0.2.0"
project.ext.versionCode= getVersionCode()

def getVersionCode() {
	//TODO: Version code should be hash of latest commit id.
    return new Date().format('yyyyMMddHH').toInteger()
}

project.ext.gitHead = 'git rev-parse --short HEAD'.execute().text.trim()